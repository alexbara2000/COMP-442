function main() => void
{
  localvar x: integer;
  localvar y: integer;
  localvar z: integer;
  x = 5;
  if (x == 5)
    then {
      x = 6;
    } else ;
  x = 1;
  y = 0;
  z = 0;
  y = x and y;
  x = x and x;
  z = x and x;

  write(x);
  write(y);
  write(z);
}