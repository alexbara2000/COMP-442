function main() => void
{
  localvar x: integer;
  localvar y: integer;
  localvar z: integer;
  x = 5;
  if (x == 5)
    then {
      x = 6;
    } else ;

  x = 1;
  y = 0;
  z = 0;

  x = x and x;
  y = x and y;
  z = x and x;

  write(x);
  write(y);
  write(z);

  x = x or 0;
  y = x or 1;
  z = 0 or 0;

  write(x);
  write(y);
  write(z);

  x = not x;
  y = not 0;

  write(x);
  write(y);

}